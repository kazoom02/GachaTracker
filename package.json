{
  "name": "gacha-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted Genshin Impact + Wuthering Waves pull tracker (Vercel).",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node tests/wuwa-merge.test.mjs && node tests/wuwa-api.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
