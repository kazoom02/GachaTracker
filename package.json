{
  "name": "gacha-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted Genshin Impact + Wuthering Waves pull tracker (Netlify).",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
